# frozen_string_literal: true

require_relative 'cutscene/script'

module RGame
  module Engine
    # Cutscenes: `Cutscene::Script` is the recipe, a list of steps, and
    # `Components::Cutscene` runs one on a node.
    module Cutscene
    end
  end
end
